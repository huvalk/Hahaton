FROM huvalk/opencv:armv7

USER root

COPY . /home/Hahaton

WORKDIR /home/Hahaton

RUN apt -y update && apt -y upgrade && apt -y install curl && apt -y install libcurl4-openssl-dev

RUN cmake .

RUN make
	
WORKDIR /home/Hahaton/app

CMD ./main
