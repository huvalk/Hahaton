FROM huvalk/opencv:armv7

USER root

COPY . /home/Hahaton

WORKDIR /home/Hahaton

RUN apt -y update && sudo apt -y upgrade && sudo apt -y install curl && sudo apt -y install libcurl4-openssl-dev

RUN cmake .

RUN make
	
WORKDIR /home/Hahaton/app

CMD ./main
