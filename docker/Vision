FROM huvalk/opencv:armv7

USER root

COPY . /home/huvalk/openCV_hakaton

WORKDIR /home/huvalk/openCV_hakaton

RUN apt -y update && apt -y upgrade && apt -y install curl && apt -y install libcurl4-openssl-dev

RUN cmake .

RUN make
	
WORKDIR /home/huvalk/openCV_hakaton/app

CMD ./main
